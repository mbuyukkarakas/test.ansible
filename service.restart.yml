---
- name: Restart chronyD  Service
  hosts: "{{ host }}"
  become: yes

  tasks:
    - name: Ensure chronyD service is running
      systemd:
        name: "{{ service }}"
        state: started
      ignore_errors: yes

    - name: Restart chronyD service
      systemd:
        name: "{{ service }}"
        state: restarted
